# 🚀 Деплой THE WORLD'S FRAME на Aeza VPS

## 📋 Что нужно:

1. **VPS на Aeza.net:**
   - Тариф: Любой (от 200₽/мес)
   - ОС: **Ubuntu 22.04**
   - RAM: минимум 1GB (лучше 2GB)
   - Диск: 10GB достаточно

2. **Доступ к серверу:**
   - IP адрес
   - SSH ключ или пароль root

---

## 🎯 Пошаговая инструкция:

### Шаг 1: Создай VPS на Aeza

1. Зайди на [aeza.net](https://aeza.net)
2. Купи VPS:
   - **Локация:** Москва или Амстердам (на выбор)
   - **ОС:** Ubuntu 22.04
   - **Тариф:** START-1 или выше (~200₽)
3. Получи:
   - IP адрес (например: `45.67.89.123`)
   - Пароль root (придет на email)

---

### Шаг 2: Подключись к серверу

**На Windows (PowerShell):**
```powershell
ssh root@45.67.89.123
# Введи пароль когда попросит
```

**Или через PuTTY** (скачать: putty.org)

---

### Шаг 3: Загрузи файлы на сервер

**Вариант А: Через Git (рекомендую)**

На сервере выполни:
```bash
cd /tmp
git clone https://github.com/YOUR_USERNAME/the_one_project.git
```

**Вариант Б: Через SCP (если нет Git репозитория)**

На твоем компе (PowerShell):
```powershell
cd C:\Users\range\Desktop
scp -r the_one_project root@45.67.89.123:/tmp/
```

---

### Шаг 4: Запусти деплой-скрипт

На сервере:
```bash
cd /tmp/the_one_project
chmod +x deploy.sh
./deploy.sh
```

⏳ Скрипт работает ~5 минут. Он автоматически:
- Установит Python, Nginx
- Создаст сервисы для бота и Flask
- Настроит автозапуск

---

### Шаг 5: Настрой .env файл

```bash
nano /opt/the_worlds_frame/.env
```

Вставь свои данные:
```env
BOT_TOKEN=8580342687:AAG_pVJZQQ93LcZ7D-NsYAyoe3yaZ8K2NKk
GOOGLE_API_KEY=AIzaSyDBsgvbyZmVP2ubqIxn55LTxztAg9k2HqA
CHANNEL_ID=@the_worlds_frame
WEBAPP_URL=http://45.67.89.123
```

**⚠️ Замени `45.67.89.123` на свой IP!**

Сохрани: `Ctrl+O`, `Enter`, `Ctrl+X`

---

### Шаг 6: Перезапусти сервисы

```bash
systemctl restart theone-bot theone-webapp
```

Проверь статус:
```bash
systemctl status theone-bot
systemctl status theone-webapp
```

Должно быть: **● active (running)** (зеленым)

---

### Шаг 7: Проверь в браузере

Открой: `http://ТВОЙ_IP` (например: `http://45.67.89.123`)

Должен открыться Mini App!

---

### Шаг 8: Настрой бота в @BotFather

В Telegram напиши **@BotFather**:

```
/setmenubutton
@the_worlds_frame_bot
👑 THE WORLD'S FRAME
http://45.67.89.123
```

---

## ✅ ГОТОВО!

Теперь бот работает 24/7 без ngrok!

---

## 🔍 Полезные команды:

### Посмотреть логи бота:
```bash
journalctl -u theone-bot -f
```

### Посмотреть логи Flask:
```bash
journalctl -u theone-webapp -f
```

### Перезапустить всё:
```bash
systemctl restart theone-bot theone-webapp nginx
```

### Остановить сервисы:
```bash
systemctl stop theone-bot theone-webapp
```

### Обновить код:
```bash
cd /opt/the_worlds_frame
git pull  # Если через Git
sudo systemctl restart theone-bot theone-webapp
```

---

## 🔒 Опционально: HTTPS (SSL)

Если хочешь HTTPS (рекомендую для продакшена):

1. **Купи домен** (например: `theone.ru` на reg.ru)
2. **Привяжи к IP** (A-запись в DNS)
3. **Установи Certbot:**

```bash
apt install certbot python3-certbot-nginx
certbot --nginx -d theone.ru
```

Готово! Теперь будет работать на `https://theone.ru`

---

## ❗ Если что-то не работает:

1. **Проверь файрволл Aeza** (должен быть открыт порт 80)
2. **Проверь .env файл** (правильные токены?)
3. **Посмотри логи:** `journalctl -u theone-bot -n 50`
4. **Проверь Nginx:** `nginx -t`

---

## 💰 Стоимость:

- **Aeza START-1:** ~200₽/месяц
- **Домен (опционально):** ~200₽/год
- **SSL:** бесплатно (Let's Encrypt)

**Итого:** ~200₽/месяц 🎉

---

## 🆘 Нужна помощь?

Скинь мне вывод команды:
```bash
journalctl -u theone-bot -n 50
```

И я помогу разобраться!

