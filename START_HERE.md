# 🚀 QUICK START GUIDE

## ✅ Ты продакт, не разработчик? Читай отсюда!

Я сделал всё за тебя. Вот что нужно для запуска:

---

## 📋 ЧТО У ТЕБЯ ЕСТЬ СЕЙЧАС

### Backend (Бэкенд):
✅ `bot.py` - Telegram бот (принимает оплату, проверяет фото)
✅ `webapp.py` - Flask сервер (API для Mini App)
✅ `database.py` - База данных SQLite
✅ `ai_check.py` - AI модерация через Gemini

### Frontend (Фронтенд):
✅ `static/index.html` - Красивая страница Mini App
✅ `static/style.css` - Дизайн
✅ `static/app.js` - Логика интерфейса

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ (5 ШАГОВ)

### Шаг 1: Установи зависимости
```bash
pip install -r requirements.txt
```

### Шаг 2: Создай файл `.env`
Скопируй `.env.example` в `.env` и заполни:

```
BOT_TOKEN=твой_токен_от_BotFather
GOOGLE_API_KEY=твой_ключ_от_Google_AI
CHANNEL_ID=-1001234567890
WEBAPP_URL=https://твой-ngrok-url.ngrok.io
```

**Где взять:**
- `BOT_TOKEN`: @BotFather в Telegram
- `GOOGLE_API_KEY`: https://makersuite.google.com/app/apikey
- `CHANNEL_ID`: Создай канал, перешли из него сообщение в @userinfobot
- `WEBAPP_URL`: Пока оставь как есть, обновишь в шаге 4

### Шаг 3: Запусти бота (Терминал 1)
```bash
python bot.py
```

Должно появиться: `Bot started!`

### Шаг 4: Запусти веб-сервер (Терминал 2)
```bash
python webapp.py
```

Должно появиться: `Flask server starting...`

### Шаг 5: Открой Mini App через ngrok (Терминал 3)
```bash
ngrok http 5000
```

**Ngrok выдаст тебе URL типа:** `https://abc123.ngrok.io`

**ВАЖНО:** Скопируй этот URL и:
1. Добавь в `.env` как `WEBAPP_URL=https://abc123.ngrok.io`
2. Перезапусти `bot.py` (Ctrl+C и снова `python bot.py`)

### Шаг 6: Настрой Mini App в BotFather
1. Открой @BotFather
2. Выбери своего бота
3. Нажми **Bot Settings** → **Menu Button** → **Edit Menu Button URL**
4. Вставь URL от ngrok: `https://abc123.ngrok.io`

---

## 🎮 ТЕСТИРУЙ!

1. Открой своего бота в Telegram
2. Нажми `/start`
3. Нажми кнопку **"🎮 Open THE ONE"**
4. Видишь Mini App? **ГОТОВО!** 🎉

---

## 🐛 Что-то не работает?

### Бот не запускается
- Проверь `BOT_TOKEN` в `.env`
- Убедись, что установил все зависимости

### Mini App не открывается
- Проверь, что Flask запущен (`python webapp.py`)
- Проверь, что ngrok запущен (`ngrok http 5000`)
- Убедись, что `WEBAPP_URL` в `.env` совпадает с URL от ngrok

### Фото не отображается
- Пока в БД нет царей, будет показана корона 👑
- После первой покупки фото появится

### AI проверка не работает
- Проверь `GOOGLE_API_KEY` в `.env`
- Убедись, что API квота не исчерпана

---

## 📱 ОСНОВНЫЕ КОМАНДЫ

### В боте:
- `/start` - Начало, открыть Mini App
- `/buy` - Быстрая покупка (без Mini App)
- `/app` - Открыть Mini App

### В Mini App:
- **Dethrone Button** - Купить трон (откроет бота)
- **Share Button** - Поделиться с друзьями
- **Hall of Fame** - Топ-10 царей

---

## 💰 ТЕСТОВЫЙ РЕЖИМ

Сейчас бот в **тестовом режиме**:
- Реальная оплата = **1 XTR** (звезда)
- UI показывает "симулированную" цену (растет на 10%)

Это нужно для тестирования. В продакшене уберешь тестовый режим.

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

После того, как всё заработало локально:

1. **Протестируй полный флоу:**
   - Оплата → Отправка фото → AI проверка → Публикация в канал

2. **Задеплой на сервер:**
   - Арендуй VPS (DigitalOcean, Hetzner, etc.)
   - Установи все зависимости
   - Настрой домен + SSL
   - Запусти через supervisor/systemd

3. **Включи продакшн:**
   - В `bot.py` строка 40-41 убери `TESTING MODE`
   - Цена начнет расти реально на 10%

---

## 📞 НУЖНА ПОМОЩЬ?

Напиши мне в Telegram или создай Issue на GitHub.

**P.S.** Весь код написан просто и понятно. Открой любой файл - внутри есть комментарии! 💡

---

## 🎯 АРХИТЕКТУРА (Упрощенно)

```
User → Telegram Bot → Оплата XTR → Фото → AI Check → БД → Канал
         ↓                                               ↓
    Mini App ← Flask API ← Database (SQLite) ← Hall of Fame
```

**Всё!** Ты готов. Удачи! 👑⭐
